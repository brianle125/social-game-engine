#pragma once

#include <fstream>

class GameCreator {
public:

	//create game from JSON
	void createGame(std::string gameSpecification);
private:

	//JSON parser
	//ask client for setup input
};